name: Deploy

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Install dependencies
      run: |
        pip install boto3 fitz tiktoken psycopg2 openai
        
    - name: Debug - List files
      run: ls -R


    - name: Zip Lambda function
      run: |
        zip -j lambda/ingestion.zip infra/lambda/lambda_function.py

    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v1

    - name: Terraform Init
      run: terraform init
     

    - name: Terraform Apply
      run: terraform apply -auto-approve
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        DB_HOST: first
        DB_NAME: first
        DB_USER: first
        DB_PASSWORD: Prashola@2407
        VPC_ID: vpc-06c1c352d16a03fe2
        SUBNET_IDS: subnet-029a8beb2b79e585c
